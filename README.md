# Kitty Config

To use these configs for your kitty terminal run the following command.  

NOTE: This will delete any prevous configurations you had; so make a copy of you ~/.config/kitty directory.

```sh
./config.sh
```
